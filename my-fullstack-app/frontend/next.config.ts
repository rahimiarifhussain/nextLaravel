/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove any experimental features you don't need
  // Sometimes experimental features can cause issues
  reactStrictMode: true,
  swcMinify: true,
};

export default nextConfig;